# Firmus Linux NVIDIA dracut configuration
# These modules are included for NVIDIA GPU support
# Safe to include on non-NVIDIA hardware — modules are only loaded if hardware present

add_drivers+=" nvidia nvidia_modeset nvidia_uvm nvidia_drm "
force_drivers+=" nvidia_drm "
install_items+=" /usr/lib/modprobe.d/nvidia.conf "
